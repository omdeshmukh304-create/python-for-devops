# -----------------------------------------------------------------------------
# Python for DevOps + Agentic AI — full course dependencies
# -----------------------------------------------------------------------------
# Installs everything used across all modules. To install only what one module
# needs, use that module's own requirements.txt instead
# (e.g. pip install -r python-foundations/requirements.txt).
#
#   pip install -r requirements.txt
# -----------------------------------------------------------------------------

# python-foundations — system metrics
psutil>=5.9.0

# apis-and-json — HTTP client
requests>=2.31.0

# aws-automation-boto3 — AWS SDK
boto3>=1.34.0

# apis-with-fastapi — internal APIs
fastapi>=0.110.0
uvicorn>=0.29.0

# ai-agents-for-devops — local AI agents (needs Ollama running separately)
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0

# testing
pytest>=8.0.0

# Note: AWS CDK (aws-cdk-lib, constructs) is optional and only needed for the
# IaC demo in aws-automation-boto3/examples/cdk_demo — install it separately.
